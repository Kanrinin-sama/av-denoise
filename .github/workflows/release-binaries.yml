name: Release binaries

on:
  release:
    types: [published]
  # A dispatch run is a dry run. It builds and smoke tests the CLI binaries, packages
  # the VapourSynth plugin, and uploads both as workflow artifacts, but never attaches
  # them to a release. Leave `version` empty to just check that everything still compiles.
  workflow_dispatch:
    inputs:
      version:
        description: "Version to build, e.g. 0.4.0-alpha4. Defaults to the Cargo.toml version."
        required: false
        type: string

permissions:
  contents: read

env:
  # The upstream MSVC build of FFMS2, the only prebuilt import library and headers
  # published for Windows. Bump FFMS2_WINDOWS_VERSION and the archive name together.
  FFMS2_WINDOWS_VERSION: "5.0"

jobs:
  resolve-version:
    name: Resolve release version
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.version.outputs.version }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Resolve and check version
        id: version
        env:
          EVENT_NAME: ${{ github.event_name }}
          RELEASE_TAG: ${{ github.event.release.tag_name }}
          DISPATCH_VERSION: ${{ inputs.version }}
        run: |
          set -euo pipefail

          cargo_version="$(python3 -c "import tomllib, pathlib; print(tomllib.loads(pathlib.Path('Cargo.toml').read_text())['workspace']['package']['version'])")"

          if [ "${EVENT_NAME}" = "release" ]; then
            version="${RELEASE_TAG}"
          else
            # A dispatch run that names no version is only checking that the targets
            # build, so it inherits whatever the tree currently says.
            version="${DISPATCH_VERSION:-${cargo_version}}"
          fi

          version="${version#v}"

          if ! [[ "${version}" =~ ^[0-9]+\.[0-9]+\.[0-9]+(-[0-9A-Za-z.-]+)?$ ]]; then
            echo "::error::'${version}' is not a valid semantic version (expected X.Y.Z or X.Y.Z-suffix)"
            exit 1
          fi

          # A mismatch is fatal for a real release, since the wrong version would ship.
          # A dispatch run attaches nothing, so it only warns and carries on building.
          if [ "${cargo_version}" != "${version}" ]; then
            message="version ${version} does not match Cargo.toml ${cargo_version}"
            if [ "${EVENT_NAME}" = "release" ]; then
              echo "::error::${message}"
              exit 1
            else
              echo "::warning::${message}"
            fi
          fi

          echo "version=${version}" >> "${GITHUB_OUTPUT}"

  build:
    name: ${{ matrix.platform }}
    needs: resolve-version
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        # One binary per platform carrying every backend that platform can reach.
        # The `Accelerator` variants are additively feature-gated, and both cudarc
        # and ash load their driver libraries at runtime, so a vulkan+cuda binary
        # runs on a host that has only one of them and `--accelerators` still picks.
        # Linux builds on 22.04 for the older glibc floor.
        #
        # The VapourSynth plugin ships from the same job, built from the same tree
        # with the same backends. `rocm` is left out of `plugin_features` for the
        # reason the wheels leave it out: it hard-links the ROCm libraries, so a
        # plugin carrying it fails to load on a host without ROCm installed.
        include:
          - os: ubuntu-22.04
            platform: linux-x86
            features: vulkan,cuda,binary
            artifact: av-denoise-dynamic-linux-x86
            binary: av-denoise
            plugin_features: vulkan,cuda
            plugin_library: libav_denoise_vs.so
            plugin_archive: av-denoise-vs-linux-x86.tar.gz
          - os: windows-latest
            platform: win64-x86
            features: vulkan,cuda,binary
            artifact: av-denoise-dynamic-win64-x86.exe
            binary: av-denoise.exe
            plugin_features: vulkan,cuda
            plugin_library: av_denoise_vs.dll
            plugin_archive: av-denoise-vs-win64-x86.zip
          - os: macos-14
            platform: macos-aarch64
            features: metal,binary
            artifact: av-denoise-dynamic-macos-aarch64
            binary: av-denoise
            plugin_features: metal
            plugin_library: libav_denoise_vs.dylib
            plugin_archive: av-denoise-vs-macos-aarch64.zip
    concurrency:
      group: release-binaries-${{ matrix.platform }}-${{ github.event.release.tag_name || inputs.version || github.run_id }}
      cancel-in-progress: false
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      # nasm builds av-scenechange's assembly, clang backs ffms2-sys's bindgen, and
      # ffms2 itself is linked dynamically, so the host supplies it at run time.
      - name: Install build dependencies (Linux)
        if: runner.os == 'Linux'
        run: |
          set -euo pipefail
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends nasm clang libclang-dev libffms2-dev

      - name: Install build dependencies (macOS)
        if: runner.os == 'macOS'
        run: brew install nasm ffms2

      - name: Install NASM (Windows)
        if: runner.os == 'Windows'
        uses: ilammy/setup-nasm@v1

      # FFMS2 ships no pkg-config file on Windows, so ffms2-sys reads the headers and
      # import library out of FFMS_INCLUDE_DIR and FFMS_LIB_DIR instead.
      - name: Install FFMS2 (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          $name = "ffms2-$env:FFMS2_WINDOWS_VERSION-msvc"
          $url = "https://github.com/FFMS/ffms2/releases/download/$env:FFMS2_WINDOWS_VERSION/$name.7z"
          Invoke-WebRequest -Uri $url -OutFile "$env:RUNNER_TEMP\ffms2.7z"
          7z x "$env:RUNNER_TEMP\ffms2.7z" -o"$env:RUNNER_TEMP\ffms2" | Out-Null
          $root = "$env:RUNNER_TEMP\ffms2\$name"
          "FFMS_INCLUDE_DIR=$root\include" | Out-File -FilePath $env:GITHUB_ENV -Append
          "FFMS_LIB_DIR=$root\x64" | Out-File -FilePath $env:GITHUB_ENV -Append
          "LIBCLANG_PATH=C:\Program Files\LLVM\bin" | Out-File -FilePath $env:GITHUB_ENV -Append

      - name: Build the CLI
        run: cargo build --release -p av-denoise --no-default-features --features ${{ matrix.features }}

      # Hosted runners have no accelerator, so this only proves the binary's own
      # dynamic libraries resolve, matching the check the release images run.
      - name: Smoke test
        shell: bash
        run: ./target/release/${{ matrix.binary }} --help > /dev/null

      - name: Name the artifact
        shell: bash
        run: mv "target/release/${{ matrix.binary }}" "${{ matrix.artifact }}"

      - name: Upload binary artifact
        uses: actions/upload-artifact@v4
        with:
          name: binary-${{ matrix.artifact }}
          path: ${{ matrix.artifact }}
          if-no-files-found: error

      - name: Build the VapourSynth plugin
        run: cargo build --release -p av-denoise-vs --no-default-features --features ${{ matrix.plugin_features }}

      # The archive holds the shared library alone, so it drops straight into a
      # VapourSynth autoload directory once unpacked. Linux gets a tarball to keep
      # the file mode, the other two get a zip.
      - name: Package the VapourSynth plugin
        shell: bash
        env:
          PLUGIN_LIBRARY: ${{ matrix.plugin_library }}
          PLUGIN_ARCHIVE: ${{ matrix.plugin_archive }}
        run: |
          set -euo pipefail
          test -f "target/release/${PLUGIN_LIBRARY}"
          cd target/release
          case "${RUNNER_OS}" in
            Linux) tar -czf "${GITHUB_WORKSPACE}/${PLUGIN_ARCHIVE}" "${PLUGIN_LIBRARY}" ;;
            macOS) zip -j "${GITHUB_WORKSPACE}/${PLUGIN_ARCHIVE}" "${PLUGIN_LIBRARY}" ;;
            Windows) 7z a -tzip "${GITHUB_WORKSPACE}/${PLUGIN_ARCHIVE}" "${PLUGIN_LIBRARY}" ;;
          esac

      - name: Upload VapourSynth plugin artifact
        uses: actions/upload-artifact@v4
        with:
          name: plugin-${{ matrix.plugin_archive }}
          path: ${{ matrix.plugin_archive }}
          if-no-files-found: error

  publish:
    name: Publish release binaries
    needs: [resolve-version, build]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Download binary artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: binary-*
          path: release-assets
          merge-multiple: true

      - name: Download VapourSynth plugin artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: plugin-*
          path: release-assets
          merge-multiple: true

      - name: List downloaded release assets
        run: ls -la release-assets

      - name: Attach release assets to the GitHub Release
        if: github.event_name == 'release'
        uses: softprops/action-gh-release@v2
        with:
          files: release-assets/*

      - name: Report the dry run
        if: github.event_name != 'release'
        env:
          VERSION: ${{ needs.resolve-version.outputs.version }}
        run: |
          set -euo pipefail
          {
            echo "### Dry run"
            echo
            echo "All three targets built their CLI binary and VapourSynth plugin at version \`${VERSION}\`,"
            echo "and each binary passed its \`--help\` smoke test."
            echo "Nothing was attached to a release. The binaries and plugin archives are on this run as workflow artifacts."
          } >> "${GITHUB_STEP_SUMMARY}"
